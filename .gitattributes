# Set default behavior for text files
* text=auto

# Treat all TypeScript files as text and use LF for line endings
*.ts text eol=lf

# Treat JavaScript, CSS, and HTML files as text and use LF for line endings
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf

# Treat image files as binary
*.png binary
*.jpg binary
*.gif binary
*.webp binary

# Treat build output files as binary
*.min.js binary
*.min.css binary

# Treat compression formats as binary
*.zip binary
*.gz binary
*.tar binary

# Exclude the stylelint directory in node_modules from text handling
node_modules/stylelint/* -text

# Treat files in the dist and build directories as binary
dist/* -text
build/* -text

# Treat YAML and JSON configuration files as text and use LF for line endings
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf

# Treat shell scripts and batch files with appropriate line endings
*.sh text eol=lf
*.bat text eol=crlf

# Treat Markdown files as text and use LF for line endings
*.md text eol=lf

# Treat SVG files as text and use LF for line endings
*.svg text eol=lf

# Treat JSX and TypeScript JSX files as text and use LF for line endings
*.jsx text eol=lf
*.tsx text eol=lf

# Treat documentation file types as text and use LF for line endings
*.txt text eol=lf
*.rtf text eol=lf

# Treat configuration files as text and use LF for line endings
*.config text eol=lf
*.conf text eol=lf

# Treat database files appropriately
*.sql text eol=lf
*.sqlite binary

# Treat audio and video files as binary
*.mp3 binary
*.mp4 binary
*.wav binary

# Specify diff behavior for certain file types
*.md diff=markdown
*.txt diff=text

# Git LFS rules (if using Git Large File Storage)
*.psd filter=lfs diff=lfs merge=lfs -text
*.ai filter=lfs diff=lfs merge=lfs -text

# Treat IDE/editor configuration files as text and use LF for line endings
.editorconfig text eol=lf
.eslintrc text eol=lf
