{
  "name": "dawn-clark-stories",
  "version": "1.0.0",
  "description": "Dawn Clark Stories website",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build && cp -R * build/ && rm -rf build/{node_modules,.github,.git}",
    "deploy": "gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "prettify": "prettier --write .",
    "lint-fix": "eslint . --fix",
    "gulp": "gulp",
    "precommit": "npm run lint-fix && npm run prettify"
  },
  "keywords": [],
  "author": "Dawn Clark",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "prettier": "^2.3.2",
    "gulp": "^4.0.2",
    "gulp-eslint": "^6.0.0",
    "gulp-prettier": "^3.0.0",
    "gh-pages": "^3.2.3"
  }
}
